


| - | - |
|---|---|
| 类型 | 原逻辑/现逻辑/备注 |
| 非个代一人式（authorityPerson） | 1. 无责任户管理权限：     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityPerson' and code = 'notControl';     2. 目前配置：editPersonInfo,transform,editTag,addRelationCustomer,sendMessage 2. 有责任户管理权限     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityPerson' and code = 'openResponsiblePower';     2. 目前配置：editPersonInfo,transform,editTag,addRelationCustomer,sendMessage |
| 企业一户式（authorityCustomer） | 1. 以下权限code为满足则取交集     1. 责任户管理相关权限         1. 目前不管走哪条线路，线上配置的都一样，都是：editCustomerInfo,allContactInfo,editTagInfo          2. 责任户管理权限关闭             1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomer' and code = 'notControl';             2. 目前配置：editCustomerInfo,allContactInfo,editTagInfo         3. 责任户管理权限开启             1. 客户无客户经理：                 1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomer' and code = 'customerManagerNone';                 2. 目前配置：editCustomerInfo,allContactInfo,editTagInfo             2. 登陆用户是客户经理或协同：                 1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomer' and code = 'customerManagerMe';                 2. 目前配置：editCustomerInfo,allContactInfo,editTagInfo     2. 面销客户经理相关权限控制         1. 有面销客户经理权限并且是客户经理             1. 有经营中心：                 1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomerFaceSale' and code = 'customerManagerMe';                 2. 目前配置：editChance             2. 无经营中心：                 1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomerFaceSale' and code = 'customerManagerMeAndAdminCompanyCodeNone';                 2. 目前配置：editChance         2. 有面销客户经理权限，是协同但是不是客户经理             1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomerFaceSale' and code = 'customerSynergyMe';             2. 目前配置：editChance         3. 有面销客户经理权限，不管是不是客户经理，只要是协同             1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomerFaceSale' and code = 'customerSynergyMeSingle';             2. 目前配置：releaseSynergy     3. 校验是否在公共池内：查询分子公司+不含下级、经营中心+不含下级、网点级，三层         1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityCustomerFaceSale' and code = 'customerManagerNoneAndMatchArea';         2. 目前配置：claim     4. 多企业相关：是客户经理或者是协同         1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityRelationCustomer' and code = 'customerManagerMeOrSynergyMe';         2. 目前配置：customerRelation     5. 补充协同认领权限         1. 查询是否有可见协同类型，逻辑是直接调用[05-查询该客户可认领大区制协同类型（含权限校验）](/read/book/70/174581)此接口         2. 如果有可见协同类型并且登陆用户非当前客户的客户经理             1. 返回配置：synergy.claim.button.code =synergyClaim     6. 补充政务合同按钮权限         1. 判断登陆人是否有政务合同按钮权限：governContractBtn =govern\_contract\_btn         2. 有政务合同按钮权限并且是客户经理或协同             1. 返回配置：userPasswordBtn =userPasswordBtn     7. 补充用户密码权限         1. 判断登陆人是否有用户密码权限：governContractBtn =govern\_contract\_btn         2. 有用户密码权限并且是客户经理或协同             1. 返回配置：userPasswordBtn =userPasswordBtn  **备注**  1. 对接市场划分新接口查询客户经理和协同     1. 查询企业客户经理划分情况：[cn.com](http://cn.com).servyou.market.facade.region.QueryRegionCustomerDivideFacade#listByCustomerIdList     2. 查询协同人员划分情况：[cn.com](http://cn.com).servyou.market.facade.region.QueryRegionCustomerSynergyDivideFacade#listByCustomerIdList 2. 校验是否在公共池内 |
| 中介一户式（authorityInstitution） | 1. 若开启销售机会权限并且客户经理是我，则返回editChance；客户经理不是我/客户经理为空，则不返回editChance     1. 详细逻辑         1. 销售机会权限：agent.person.action.bars.code =agent\_person\_edit\_chance         2. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityPersonAgentWithSaleChance';         3. 客户经理是我：目前配置 editChance         4. 客户经理不是我/客户经理为空：目前配置为空 2. 判断责任户管理权限是否开启，未开启/开启客户经理是我/开启客户经理为空 都返回orderRecord,fatherSonRelationship,allContactInfo,addFollowRecord,addAgentCustomer,exportList,managerAccounting     1. 详细逻辑         1. 未开启责任户管理权限             1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityInstitution' and code = 'notControl';             2. 目前配置：orderRecord,fatherSonRelationship,allContactInfo,addFollowRecord,addAgentCustomer,exportList,managerAccounting         2. 开启责任户管理权限             1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityInstitution';             2. 客户经理是我/客户经理为空：orderRecord,fatherSonRelationship,allContactInfo,addFollowRecord,addAgentCustomer,exportList,managerAccounting             3. 客户经理是他人：配置了空 3. 获取登陆人团队管理权限范围下配置的组织机构ID列表，如果客户的客户经理所属部门包含在内，则认为登陆人是有数据权限的管理者     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityInstitutionForSaleChance';     2. 客户经理是我/无客户经理/有数据权限的管理者，则返回showSaleChanceTag 4. 主从关系：维护主从机构按钮     1. 逻辑：（ (【是机构的客户经理】且【有维护主从机构权限】） 或 【有中台维护主从机构权限】)且【非从机构】则返回maintain.master.slave.code=maintainMasterAndSlave  **备注**  1. 对接市场划分新接口查询客户的客户经理划分情况：[cn.com](http://cn.com).servyou.market.facade.region.QueryRegionCustomerDivideFacade#listByCustomerIdList |
| 个代一户式（authorityAgent） | 1. 若开启销售机会权限并且客户经理是我，则返回editChance；客户经理不是我/客户经理为空，则不返回editChance     1. 详细逻辑         1. 销售机会权限：agent.person.action.bars.code =agent\_person\_edit\_chance         2. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityPersonAgentWithSaleChance';         3. 客户经理是我/协同客户经理是我：目前配置 editChance         4. 客户经理不是我/客户经理为空：目前配置为空 2. 如果登陆用户是客户的协同客户经理     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityAgent' and code = 'customerSynergyMeSingle';     2. 目前配置：releaseSynergy 3. 判断是否开启责任户管理权限     1. 没有开启责任户管理权限         1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityAgent' and code = 'notControl';         2. 目前配置：editPersonInfo,transform,editTag,addRelationCustomer,sendMessage,editAgentInfo,fatherSonRelationship,addAgentCustomer,exportList,maintainContact,openAccount         3. 加上上面第三点和第四点（销售机会、释放协同），后直接返回     2. 开启责任户管理权限         1. 在上面第三点和第四点（销售机会、释放协同）的基础上再补充以下code         2. 查询登陆用户和当前客户的关系             1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE \`key\` = 'authorityAgent' ;             2. 客户经理是我/协同客户经理是我/没有客户经理：editPersonInfo,transform,editTag,sendMessage,editAgentInfo,fatherSonRelationship,addAgentCustomer,exportList,maintainContact,openAccount             3. 客户经理是他人：editTag,sendMessage         3. 查询个代对应的主用户             1. 调用客户中心接口：[cn.com](http://cn.com).servyou.operationcustomer.facade.PersonCustomerRelationFacade#getMainPersonListByCustomerIdList             2. 如果个代主用户不存在或个代主用户有两个及以上，则直接返回上面获取到的结果，不再继续往下处理逻辑             3. 如果个代主用户存在并且只有一个                 1. SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityPersonAgentWithPerson';                 2. 个代主用户的客户经理为空/是我：返回addRelationCustomer，新增自主企业                 3. 个代主用户的客户经理是他人则不返回 4. 补充认领权限校验是否在公共池内 5. 补充认领协同，调用同http接口 /customer/region/visibleSynergy一个方法，判断是否有客户可以认领的协同类型，有的话补充认领协同按钮：agent.synergy.claim.button.function.code =agentClaimSynergy  **备注**  1. 对接市场划分新接口查询客户的客户经理划分情况：[cn.com](http://cn.com).servyou.market.facade.region.QueryRegionCustomerDivideFacade#listByCustomerIdList 2. 对接市场划分新接口查询协同人员划分情况：[cn.com](http://cn.com).servyou.market.facade.region.QueryRegionCustomerSynergyDivideFacade#listByCustomerIdList 3. 查询个代对应的主用户直接删除，直接变成，开启责任户管理权限的时候使用返回该按钮code：addRelationCustomer，也不考虑主用户是否只有一个，这种本来就是异常数据 |
| 新机构一户式（authorityNewInstitution） | 1. 以下配置按钮所有人可见：     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityNewInstitution' AND code = 'notControl';     2. 目前配置：edit,addFollowRecord,sign,order 2. 校验当前登陆用户是否为客户经理     1. 是客户经理         1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityNewInstitution' AND code = 'customerManagerMe';         2. 目前配置：saleChanceBtn,todoBtn     2. 不是客户经理         1. 如果客户的客户经理部门在登陆人团队管理下配置的组织机构id范围内则展示待办按钮         2. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityNewInstitution' AND code = 'hasDataPermission';         3. 目前配置：todoBtn 3. 登陆人有认领权限并且客户没有客户经理     1. 个代用agentClaim+claim查询权限，中介用institutionClaim+claim查询权限     2. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityNewInstitution' AND code = 'customerManagerNoneAndHasClaimPermission';     3. 目前配置：claim  **备注**  1. 校验是否客户经理需要对接市场划分新接口：[cn.com](http://cn.com).servyou.market.facade.region.QueryRegionCustomerDivideFacade#listByCustomerIdList 2. 其他逻辑都保持不变，都是用是否客户经理在判断，之前每个判断的地方都调用一次市场划分接口获取客户经理，这次改为了只调用一次接口获取划分信息 |
| 人资机构一户式（authorityPtcrmInstitution） | 1. 如果当前登陆用户是客户的客户经理     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityPtcrmInstitution' AND code = 'customerManagerMe';     2. 目前配置：edit,allContactInfo,addFollowRecord,saleChanceBtn,sign,editTag,release 2. 不满足2，即不满足「当前登陆用户是客户的客户经理」，但是协同客户经理是我     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityPtcrmInstitution' AND code = 'customerSynergyMe';     2. 目前配置：edit,allContactInfo,addFollowRecord,saleChanceBtn,sign,editTag 3. 不满足2和3，但是客户的客户经理或者协同的部门在登陆人团队管理权限范围内     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityPtcrmInstitution' AND code = 'principalMe';     2. 目前配置：edit,editTag,allContactInfo 4. 不满足2、3、4，则作为兜底逻辑存在     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityPtcrmInstitution' AND code = 'notControl';     2. 目前配置：allContactInfo 5. 客户无客户经理时，判断是否包含中介客户认领权限「institutionClaimFunctionCode =institutionClaim」，如果有权限     1. 查询语句：SELECT \* FROM itcrm\_system\_config WHERE\`key\` = 'authorityPtcrmInstitution' AND code = 'customerManagerNoneAndHasClaimPermission';     2. 目前配置：claim  **备注**  1. 查询客户客户经理和协同客户经理调用市场划分**新**接口 |
| 多企业（authorityBusinessEntity） | [多企业一户式责任户相关控制](/read/book/167/200436) |